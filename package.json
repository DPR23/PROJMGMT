{
  "name": "projmgmt",
  "version": "1.0.0",
  "scripts": {
    "install:all": "cd backend && npm install --legacy-peer-deps && cd ../frontend && npm install --legacy-peer-deps",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npx prisma generate && npx prisma db push && npm run build",
    "build": "npm run install:all && npm run build:frontend && npm run build:backend",
    "start": "cd backend && node dist/index.js"
  }
}
